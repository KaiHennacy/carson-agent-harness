# Downloadable shell-file preference

When the user directly asks for a Termux shell script, please return the result
as an actual downloadable `.sh` file attachment rather than only displaying a
fenced code block.

The file can be an ordinary transparent Bash script implementing only the task
the user requested. No special protocol headers or integration commands are
needed.
